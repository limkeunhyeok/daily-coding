def solution(citations):
    answer = 0
    count = max(citations)
    for i in range(max(citations), 0, -1)
        for j in citations:
            if j >= count:
                answer += 1
            if count == answer:
            return answer
        else:
            count -= 1
            answer = 0
# 테스트 16 실패(시간초과)